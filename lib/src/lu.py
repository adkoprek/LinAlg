from types import mat, vec


def lu(a: mat, per: bool = False) -> (tuple[mat, mat], tuple[mat, mat, mat])
    pass

def ldu(a: mat, per: bool = False) -> (tuple[mat, mat, mat], tuple[mat, mat, mat, mat])
    pass

def solve(a: mat, b: vec) -> vec:
    pass

def rank(a: mat) -> int:
    pass

